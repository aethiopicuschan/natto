# stun examples

## Client

[client example](./client)

This example demonstrates how to use the `stun` package to discover the public IP address and port mapping of a client behind a NAT using a STUN server.

## Server

[server example](./server)

This example demonstrates how to set up a simple STUN server using the `stun` package to respond to STUN binding requests from clients.
